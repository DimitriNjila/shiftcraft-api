# Migrations

There is no migration runner wired up for this project. Files here are plain
SQL, numbered in the order they should be applied. Run each one manually
against the Supabase project via the SQL editor (or `psql`), in filename order.
